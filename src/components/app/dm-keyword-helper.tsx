// STUB — owned by the growth work package (M6). Rendered inside the product editor.
export function DmKeywordHelper(_props: { username: string; slug: string; keyword: string | null; title: string }) {
  return null;
}
