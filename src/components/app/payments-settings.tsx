// STUB — owned by the Stripe Connect work package (M4). Rendered inside /app/settings.
export function PaymentsSettings(_props: { storeId: string }) {
  return (
    <div className="rounded-lg border border-dashed p-6 text-sm text-muted-foreground">
      Connect Stripe and PayPal here. Coming in the payments milestone.
    </div>
  );
}
