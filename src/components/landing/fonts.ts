import { Manrope } from "next/font/google";

/** Headline face for marketing pages; the wordmark loads its own weight. */
export const manrope = Manrope({ subsets: ["latin"], weight: ["700", "800"], variable: "--font-manrope" });
