// STUB — owned by the growth work package (M6). Rendered on every public storefront page.
// Contract: reads META_PIXEL_ID (via a server wrapper) and fires PageView; exposes
// `trackPixel(event, params)` for ViewContent / Lead / Purchase from client components.
export function MetaPixel(_props: { pixelId?: string | null }) {
  return null;
}

export function trackPixel(_event: string, _params?: Record<string, unknown>) {}
